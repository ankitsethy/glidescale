---
description: default agent behaviour policies for this project
---

// turbo-all

## Terminal Command Policy
All terminal/shell commands are pre-approved to run automatically without asking for user confirmation.

## Task Review Policy
All generated implementation plans and task artifacts should auto-proceed without waiting for explicit user approval (ShouldAutoProceed = true).
